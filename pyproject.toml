[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "dvrp"
version = "0.1.0"
description = "Dynamic Vehicle Routing Problem solver"

[tool.setuptools]
packages = ["dvrp"]

[tool.setuptools.package-dir]
"" = "src"
